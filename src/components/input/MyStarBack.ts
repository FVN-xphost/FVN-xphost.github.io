import { LitElement, html, css, unsafeCSS } from "lit";
import { customElement, property } from "lit/decorators.js";

@customElement("my-back-star")
export class MyBackStar extends LitElement {
    static styles = css``
    render() {
        return html`
        
        
        `
    }
}